<!-- 
// if (isset($_POST['crear_carpeta'])) {
//   $nombre_usuario = "alvaro"; // Reemplaza "nombre_del_usuario" con el nombre del usuario actual

//   // Crea la carpeta con el nombre del usuario
//   $ruta_carpeta = "ruta/a/la/carpeta/" . $nombre_usuario; // Reemplaza "ruta/a/la/carpeta/" con la ruta donde deseas crear la carpeta
//   mkdir($ruta_carpeta);

//   // Sube la foto a la carpeta
//   $ruta_foto = $ruta_carpeta . "/" . basename($_FILES["foto"]["name"]);
//   move_uploaded_file($_FILES["foto"]["tmp_name"], $ruta_foto);

//   echo "Carpeta y foto creadas con éxito.";
// }
//  -->
